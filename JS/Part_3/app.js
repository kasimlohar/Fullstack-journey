// ==========================================
// String Methods Examples (Part 3)
// ==========================================

let msg = "    help!    ";
console.log("Trimmed message:", msg.trim()); // Removes extra whitespace

let name1 = "KasimLohar";
console.log("Slice (characters 4 to 9):", name1.slice(4, 9)); // Extracts a substring
console.log("Index of 'Lo':", name1.indexOf("Lo")); // Finds the starting index of "Lo"
console.log("After replacing 'Kasim' with 'Saghir':", name1.replace("Kasim", "Saghir")); // Replaces substring
